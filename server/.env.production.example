# ===========================================
# RAILWAY ENVIRONMENT VARIABLES
# Copy these to Railway Dashboard → Variables
# ===========================================

# Server Configuration
NODE_ENV=production
PORT=$PORT

# MongoDB Atlas Connection (REQUIRED - replace with your Atlas URI)
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/conversaiq?retryWrites=true&w=majority

# CORS Configuration (REQUIRED - set after frontend deployment)
CLIENT_URL=https://your-app.vercel.app
ALLOWED_ORIGINS=https://your-app.vercel.app,https://conversaiq.vercel.app

# JWT Secret for authentication
JWT_SECRET=your-super-secure-secret-key-change-this-in-production

# Google Gemini AI (Optional - for AI features)
GEMINI_API_KEY=your-gemini-api-key

# IMAP Email Integration (Optional)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=your-email@gmail.com
IMAP_PASSWORD=your-app-password
IMAP_POLL_INTERVAL=60000
